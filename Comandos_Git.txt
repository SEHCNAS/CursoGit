git ini 
	- para iniciar o git na pasta selecionada

git remote add origin + Url 
	- Defini o ambiente remoto do git

git remote get-url origin 
	- Mostra qual ambiente esta sendo usado

git config --local user.name
	- Defini o nome que sera usado

git config --local user.email 
	- Defini o email que sera usado 

git config --global alias.<Comando curto><Comando longo>
	- Defini um atalho para algum comando

git <Comando> --help ou git <Comando> -help
	- Abre a documentação do comando especificado

git add <nome do arquivo>
	-Adiciona o arquivo para ser enviado no commit

git commit -m "Mensagem obrigatoria"
	- prepara os arquivo para serem enviados

git push --set-upstream origin master
	- Comando para realizar o envio para o github 

git cat-file -t <hash>
	-Mostra oque é a hase

git cat-file -p <hash>
	-Mostra mostra o conteudo da hase

git pull
	-Pega as alterações remotas e baixa localmente

git .git/head
	-Retorna a branch onde esta sendo feitos os commits 

git checkout -b <nome da nova branch>
	-Cria uma nova branch baseada na master e ja a defini para futuros commits
	-Ao criar um branch é preciso criar uma branch de rastreamento com git push --set-upstream origin <nome da branch> ou git push -u origin <nome da branch>

git branch <nome da nova branch>
	-Cria a branch sem alterar automaticamente para ela

git branch -c <nome da branch específica> <nome da nova branch>
	-Para criar uma nova branch a partir de uma branch especifica utilize o comando

git branch -a
	- mostra todas as branchs usadas e branchs de rastreamento


Hirarquia
	- System > Global > Local